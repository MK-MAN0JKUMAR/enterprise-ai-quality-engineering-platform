"""
Shared provider configuration type aliases.
"""

from __future__ import annotations

type ApiKey = str

type BaseUrl = str

type Endpoint = str

type ModelName = str

type OrganizationId = str

type ProjectId = str
